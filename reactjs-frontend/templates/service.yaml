apiVersion: v1
kind: Service
metadata:
  name: {{ include "reactjs-frontend.fullname" . }}
  labels:
    {{- include "reactjs-frontend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "reactjs-frontend.selectorLabels" . | nindent 4 }}
